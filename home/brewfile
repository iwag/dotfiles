#brew install brew-cask
tap caskroom/cask || true
tap caskroom/versions || true
tap homebrew/versions|| true
tap caskroom/drivers || true

# tap phinze/homebrew-cask || true
tap caskroom/fonts 
tap iwag/versions|| true
update || true

install brew-cask || true
install git || true
install gist || true
# install gibo || true
install hub || true
install byobu || true
# install wemux || true
# install tree || true
install zsh || true
# install wget || true
install curl || true
install nkf || true
install zsh-completions || true
install lv || true
# install gcc47 || true
install nodebrew || true
install nvim || true

# install llvm || true
install jq || true
install go || true
install pt || true
install ruby-build || true
install rbenv || true
install python3 || true
install pipenv || true
# install heroku-toolbelt || true
# install dub || true
install sbt || true
install maven || true
install gradle || true
install typesafe-activator || true

cask install karabiner || true
# if high sierra, cask install karabiner-elements || true
# cask install dropbox || true
# cask install bathyscaphe || true
cask install Chromium || true
cask install github || true
cask install alfred || true
cask install sourcetree || true
cask install selfcontroll || true
# cask install google-chrome || true
# cask install firefox || true
cask install virtualbox || true
cask install docker-machine || true
# cask install vmware-fusion || true
# cask install intellij-idea-community || true
# cask install totalspaces || true
# cask install totalfinder || true
cask install iterm2 || true
# cask install light-table || true
# cask install soundflower || true
cask install appcleaner || true
cask install gyazo || true
cask install visual-studio-code || true
# cask install google-japanese-ime || true
cask install java || true
cask install slack || true
# /Library/Caches/Homebrew/sequel-pro-1.0.2.dmg
# chrome-remote-desktop
cask install osxfuse veracrypt || true
cask install tunnelblick || true
cask install openoffice || true
cask install vlc || true
cask install visual-studio-code
# couldn't now, cask install vcp-ftdi-driver
brew cask install font-source-code-pro

tap peco/peco || true
install peco || true
install migemogrep || true

# following commands are not brew 
# curl -L http://install.ohmyz.sh | sh
# curl https://raw.githubusercontent.com/Shougo/neobundle.vim/master/bin/install.sh | sh
# sudo /usr/bin/local/gem install pry rb-readline homesick
# gem install rouge
# go get github.com/lestrrat/ghq
#

